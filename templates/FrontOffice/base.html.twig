<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Site public{% endblock %}</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">

    {% block stylesheets %}{% endblock %}
  </head>

  <body class="bg-light">

    {# Header public simple #}
    <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm">
      <div class="container">
        <a class="navbar-brand d-flex align-items-center" href="{{ path('app_public_site') }}">
          <img src="{{ asset('img/logo.png') }}" alt="Elder HealthCare" height="42" class="me-2">
          <span class="fw-bold text-primary">Elder</span>
          <span class="fw-bold text-warning ms-1">HealthCare</span>
        </a>

        <div class="ms-auto d-flex gap-2">
          <a class="btn btn-outline-primary" href="{{ path('app_home') }}">
            Accueil app
          </a>
          <a class="btn btn-primary" href="{{ path('app_dashboard') }}">
            Dashboard
          </a>
        </div>
      </div>
    </nav>

    {% block body %}{% endblock %}

    <footer class="py-4 mt-5 bg-white border-top">
      <div class="container text-center text-muted">
        © {{ "now"|date("Y") }} Elder HealthCare
      </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    {% block javascripts %}{% endblock %}
  </body>
</html>
