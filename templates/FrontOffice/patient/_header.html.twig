
{% block stylesheets %}
	<link rel="stylesheet" href="{{ asset('css/patient.css') }}?v=3">
{% endblock %}
<header class="topbar">
    <div class="topbar-inner">

        <!-- BRAND -->
        <a href="{{ path('app_public_site') }}" class="brand">
            <img src="{{ asset('img/logo.png') }}" alt="Elder HealthCare">
            <span class="brand-text">
                <span class="fw-bold text-primary">Elder</span>
                <span class="fw-bold text-warning ms-1">HealthCare</span>
            </span>
        </a>

        <!-- NAV -->
        <nav class="top-nav">
            <a href="{{ path('app_patient_interfce') }}">Accueil</a>
            <a>Rendez-vous</a>
            <a>Consultations</a>
            <a href="{{ path('equipment_list_index') }}">Équipements</a>
            <a class="active" href="{{ path('forum_post_forum_index') }}">Forum</a>
            <a href="{{ path('front_events_index') }}">Événements</a>
            <a>Messages</a>
            <a>Alertes</a>
        </nav>

        <!-- PROFILE -->
        <div class="profile">
            <img src="{{ asset('img/home.jpg') }}" alt="Profil">
            <strong>{{ app.user.prenom }} {{ app.user.nom }}</strong>
        </div>

    </div>
</header>
