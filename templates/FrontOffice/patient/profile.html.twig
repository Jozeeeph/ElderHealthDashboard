{% block stylesheets %}
	<link rel="stylesheet" href="{{ asset('css/patient.css') }}?v=2">
{% endblock %}

{% block body %}
	<div class="patient-app">
		<header class="topbar">
			<div class="topbar-inner">
				<a href="{{ path('app_public_site') }}" class="brand">
					<img src="{{ asset('img/logo.png') }}" alt="Elder HealthCare">
					<span class="brand-text">
						<span class="fw-bold text-primary">Elder</span>
						<span class="fw-bold text-warning ms-1">HealthCare</span>
					</span>
				</a>

				<nav class="top-nav">
					<a href="{{ path('app_patient_interfce') }}">Accueil</a>
					<a href="{{ path('patient_consultations') }}">Consultations</a>
					<a href="{{ path('equipment_list_index') }}">Equipement</a>
					<a href="{{ path('forum_post_forum_index') }}">Forum</a>
					<a href="{{ path('front_events_index') }}">Événements</a>
				</nav>

				<div class="profile">
					<div class="profile-emoji">🙂</div>
					<strong>{{ patient.prenom ?? 'Patient' }}</strong>
				</div>
			</div>
		</header>

		<main class="patient-main">
			<section class="profile-hero">
				<div>
					<h1>👤 Mon profil</h1>
					<p>Un espace clair et soigné pour vos informations personnelles.</p>
				</div>
				<div class="profile-badge">💖 Profil patient</div>
			</section>

			<section class="profile-shell">
				<div class="profile-card">
					<h2>📝 Informations</h2>
					{% include 'FrontOffice/patient/_profile_form.html.twig' with { form: form } %}
					<div class="actions" style="margin-top: 12px;">
						<a class="btn ghost" href="{{ path('app_patient_interfce') }}">⬅️ Retour</a>
					</div>
				</div>

				<div class="profile-side">
					<div class="profile-mini">
						<div class="mini-title">✨ Conseils</div>
						<ul class="mini-list">
							<li>Gardez votre email à jour.</li>
							<li>Ajoutez un numéro valide.</li>
							<li>Vérifiez la date de naissance.</li>
						</ul>
					</div>
					<div class="profile-mini">
						<div class="mini-title">🔒 Sécurité</div>
						<p class="mini-text">Vos informations sont protégées et visibles uniquement par vous.</p>
					</div>
				</div>
			</section>
		</main>
	</div>
{% endblock %}
