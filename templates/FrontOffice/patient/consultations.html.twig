<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Mes consultations</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" rel="stylesheet">
    <link rel="stylesheet" href="{{ asset('css/patient.css') }}?v=20260221-health-cards">
</head>

{% block body %}
<div class="patient-app">
    <style>
        .consult-page {
            max-width: 1100px;
            margin: 0 auto;
            padding: 14px;
        }

        .health-hero {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            flex-wrap: wrap;
            gap: 10px;
            background: linear-gradient(130deg, #0f766e 0%, #0891b2 55%, #2563eb 100%);
            border-radius: 14px;
            padding: 14px;
            color: #f8fafc;
            margin-bottom: 10px;
        }

        .health-hero h1 {
            margin: 0 0 4px;
            font-size: 1.2rem;
            font-weight: 800;
        }

        .health-hero p {
            margin: 0;
            font-size: 0.9rem;
            opacity: 0.95;
        }

        .hero-chip {
            display: inline-flex;
            align-items: center;
            gap: 6px;
            border: 1px solid rgba(255, 255, 255, 0.25);
            background: rgba(255, 255, 255, 0.13);
            border-radius: 999px;
            padding: 6px 10px;
            font-size: 0.8rem;
            font-weight: 700;
        }

        .summary-grid {
            display: grid;
            grid-template-columns: repeat(3, minmax(0, 1fr));
            gap: 10px;
            margin-bottom: 10px;
        }

        .summary-card {
            background: #fff;
            border: 1px solid #dbe7ee;
            border-radius: 10px;
            padding: 10px 12px;
        }

        .summary-label {
            font-size: 0.72rem;
            text-transform: uppercase;
            color: #64748b;
            letter-spacing: 0.05em;
            font-weight: 700;
        }

        .summary-value {
            margin-top: 2px;
            font-size: 1.05rem;
            color: #0f172a;
            font-weight: 800;
        }

        .consult-card {
            background: #fff;
            border: 1px solid #dbe7ee;
            border-radius: 10px;
            padding: 12px;
            display: grid;
            grid-template-columns: 1.15fr 1fr;
            gap: 12px;
            margin-bottom: 10px;
        }

        .card-title-row {
            display: flex;
            align-items: center;
            gap: 8px;
            margin-bottom: 8px;
        }

        .card-index {
            min-width: 28px;
            height: 28px;
            border-radius: 999px;
            background: #e0e7ff;
            color: #1e40af;
            font-weight: 800;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            font-size: 0.8rem;
        }

        .card-title {
            margin: 0;
            color: #0f172a;
            font-size: 1rem;
            font-weight: 800;
        }

        .info-grid {
            display: grid;
            grid-template-columns: 150px 1fr;
            gap: 7px 10px;
        }

        .info-label {
            color: #475569;
            font-size: 0.84rem;
            font-weight: 700;
        }

        .info-value {
            color: #0f172a;
            font-weight: 800;
        }

        .doc-col {
            border-left: 1px solid #e2e8f0;
            padding-left: 12px;
            display: grid;
            gap: 10px;
            align-content: start;
        }

        .doc-row {
            display: grid;
            grid-template-columns: 1fr auto;
            gap: 8px;
            align-items: center;
            border-bottom: 1px solid #edf2f7;
            padding-bottom: 8px;
        }

        .doc-row:last-child {
            border-bottom: none;
            padding-bottom: 0;
        }

        .doc-title {
            font-weight: 700;
            color: #0f172a;
            font-size: 0.94rem;
        }

        .pager-box {
            background: #fff;
            border: 1px solid #dbe7ee;
            border-radius: 10px;
            padding: 10px 12px;
        }

        .empty-box {
            background: #fff;
            border: 1px dashed #cbd5e1;
            border-radius: 10px;
            padding: 12px;
            color: #64748b;
        }

        @media (max-width: 900px) {
            .summary-grid {
                grid-template-columns: 1fr;
            }

            .consult-card {
                grid-template-columns: 1fr;
            }

            .doc-col {
                border-left: none;
                border-top: 1px solid #e2e8f0;
                padding-left: 0;
                padding-top: 10px;
            }

            .doc-row {
                grid-template-columns: 1fr;
                justify-items: start;
            }
        }
    </style>

    {% include 'FrontOffice/patient/_header.html.twig' %}

    <main class="consult-page">
        <section class="health-hero">
            <div>
                <h1>Mes consultations</h1>
                <p>Suivi medical personnel: historique, rapports et prescriptions.</p>
            </div>
            <div class="hero-chip"><i class="bi bi-shield-check"></i> Donnees medicales securisees</div>
        </section>

        <section class="summary-grid">
            <article class="summary-card">
                <div class="summary-label">Total consultations</div>
                <div class="summary-value">{{ totals.consultations|default(pagination.totalItemCount|default(consultations|length)) }}</div>
            </article>
            <article class="summary-card">
                <div class="summary-label">Total rapports</div>
                <div class="summary-value">{{ totals.rapports|default(0) }}</div>
            </article>
            <article class="summary-card">
                <div class="summary-label">Total prescriptions</div>
                <div class="summary-value">{{ totals.prescriptions|default(0) }}</div>
            </article>
        </section>

        {% for message in app.flashes('medication_success') %}
            <div class="alert alert-success" role="alert" style="margin-top: 10px;">{{ message }}</div>
        {% endfor %}
        {% for message in app.flashes('medication_error') %}
            <div class="alert alert-danger" role="alert" style="margin-top: 10px;">{{ message }}</div>
        {% endfor %}

        {% for consultation in consultations %}
            <section class="consult-card">
                <div>
                    <div class="card-title-row">
                        <span class="card-index">{{ ((pagination.currentPageNumber|default(1) - 1) * (pagination.itemNumberPerPage|default(4))) + loop.index }}</span>
                        <h2 class="card-title"><i class="bi bi-heart-pulse me-1"></i>Consultation</h2>
                    </div>
                    <div class="info-grid">
                        <div class="info-label"><i class="bi bi-calendar3"></i> Date</div>
                        <div class="info-value">{{ consultation.dateConsultation ? consultation.dateConsultation|date('d/m/Y') : '-' }}</div>

                        <div class="info-label"><i class="bi bi-clock"></i> Heure</div>
                        <div class="info-value">{{ consultation.heureConsultation ? consultation.heureConsultation|date('H:i') : '-' }}</div>

                        <div class="info-label"><i class="bi bi-journal-medical"></i> Type</div>
                        <div class="info-value">{{ consultation.typeConsultation ?: '-' }}</div>

                        <div class="info-label"><i class="bi bi-geo-alt"></i> Lieu</div>
                        <div class="info-value">{{ consultation.lieu ?: '-' }}</div>

                        <div class="info-label"><i class="bi bi-person-badge"></i> Personnel</div>
                        <div class="info-value">{{ consultation.personnelMedical ? (consultation.personnelMedical.prenom ~ ' ' ~ consultation.personnelMedical.nom) : '-' }}</div>
                    </div>
                </div>

                <div class="doc-col">
                    <div class="doc-row">
                        <div class="doc-title"><i class="bi bi-file-earmark-medical"></i> Rapport medical</div>
                        {% if consultation.rapportMedical %}
                            <a class="btn btn-outline-primary btn-sm" href="{{ path('patient_rapport_show', {'id': consultation.rapportMedical.idRapport}) }}">Voir rapport</a>
                        {% else %}
                            <span class="text-muted small">Aucun rapport</span>
                        {% endif %}
                    </div>
                    <div class="doc-row">
                        <div class="doc-title"><i class="bi bi-capsule-pill"></i> Prescription</div>
                        {% if consultation.prescription %}
                            <a class="btn btn-primary btn-sm" href="{{ path('patient_prescription_show', {'id': consultation.prescription.idPrescription}) }}">Voir prescription</a>
                        {% else %}
                            <span class="text-muted small">Aucune prescription</span>
                        {% endif %}
                    </div>
                </div>
            </section>
        {% else %}
            <div class="empty-box">Aucune consultation disponible.</div>
        {% endfor %}

        {% if pagination is defined and pagination.pageCount > 1 %}
            <section class="pager-box mt-2 d-flex flex-column flex-md-row justify-content-between align-items-md-center gap-2">
                <div class="text-muted small">Page {{ pagination.currentPageNumber }} / {{ pagination.pageCount }} - {{ pagination.totalItemCount }} consultation(s)</div>
                <nav aria-label="Pagination consultations patient">
                    <ul class="pagination mb-0">
                        <li class="page-item {{ pagination.currentPageNumber <= 1 ? 'disabled' : '' }}">
                            <a class="page-link" href="{{ path('patient_consultations', { page: pagination.currentPageNumber - 1 }) }}">Precedent</a>
                        </li>
                        {% for p in 1..pagination.pageCount %}
                            <li class="page-item {{ p == pagination.currentPageNumber ? 'active' : '' }}">
                                <a class="page-link" href="{{ path('patient_consultations', { page: p }) }}">{{ p }}</a>
                            </li>
                        {% endfor %}
                        <li class="page-item {{ pagination.currentPageNumber >= pagination.pageCount ? 'disabled' : '' }}">
                            <a class="page-link" href="{{ path('patient_consultations', { page: pagination.currentPageNumber + 1 }) }}">Suivant</a>
                        </li>
                    </ul>
                </nav>
            </section>
        {% endif %}
    </main>
</div>
{% endblock %}
</html>
