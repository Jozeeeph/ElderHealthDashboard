{% block stylesheets %}
	<link rel="stylesheet" href="{{ asset('css/infermier.css') }}">
{% endblock %}
{% block body %}
	<div class="nurse-app">
		{% include 'FrontOffice/infermier/_header.html.twig' %}

		<main class="nurse-main">
			<section class="card rapport-card">
				<div class="rapport-split">
					<div class="rapport-left">
						<div class="rapport-title">💊 Prescription</div>
						<div class="consultation-meta">
							<span class="consultation-chip">🧾 Consultation #{{ consultation.id }}</span>
							<span class="consultation-chip">🧑‍🦳 Patient: {{ consultation.patient.prenom }} {{ consultation.patient.nom }}</span>
							<span class="consultation-chip">🩺 Type: {{ consultation.typeConsultation }}</span>
							<span class="consultation-chip">📅 Date: {{ consultation.dateConsultation ? consultation.dateConsultation|date('d/m/Y') : '-' }}</span>
							<span class="consultation-chip">⏰ Heure: {{ consultation.heureConsultation ? consultation.heureConsultation|date('H:i') : '-' }}</span>
						</div>
					</div>
					<div class="rapport-actions">
						<a class="btn-sm primary" href="{{ path('front_infermier_prescription_edit', {'id': prescription.idPrescription}) }}">Modifier</a>
						<a class="btn-sm" href="{{ path('front_infermier_prescription_pdf', {'id': prescription.idPrescription}) }}">PDF</a>
						<a class="btn-sm" href="{{ path('front_infermier_consultation_index') }}">Retour</a>
					</div>
				</div>
			</section>

			<section class="card rapport-card">
				<div class="rapport-split rapport-split-top">
					<div class="rapport-left">
						<div class="rapport-title">🧪 Details de la prescription</div>
						<div class="consultation-meta">
							<span class="consultation-chip">🔁 Frequence: {{ prescription.frequence }}</span>
							<span class="consultation-chip">💧 Dosage: {{ prescription.dosage }}</span>
							<span class="consultation-chip">⏳ Duree: {{ prescription.dureeTraitement }}</span>
							<span class="consultation-chip">
								🗓️ Periode: {{ prescription.dateDebut ? prescription.dateDebut|date('d/m/Y') : '-' }}
								-
								{{ prescription.dateFin ? prescription.dateFin|date('d/m/Y') : '-' }}
							</span>
						</div>
					</div>
				</div>
				<div class="item">
					<strong>💊 Medicaments</strong>
					<p>{{ prescription.medicaments }}</p>
				</div>
				<div class="item">
					<strong>🧭 Consignes</strong>
					<p>{{ prescription.consignes ?: '-' }}</p>
				</div>
				<div class="consultation-actions">
					<form method="post" action="{{ path('front_infermier_prescription_delete', {'id': prescription.idPrescription}) }}"
						onsubmit="return confirm('Supprimer cette prescription ?');">
						<input type="hidden" name="_token" value="{{ csrf_token('delete_prescription_' ~ prescription.idPrescription) }}">
						<button class="btn-sm danger" type="submit">Supprimer</button>
					</form>
				</div>
			</section>
		</main>
	</div>
{% endblock %}

