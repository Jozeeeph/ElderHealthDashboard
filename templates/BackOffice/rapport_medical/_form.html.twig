{{ form_start(form, {
    action: form_action|default(app.request.uri)
}) }}
    <div class="row g-3">
        <div class="col-12">
            {{ form_row(form.diagnostic, { label: '🩺 Diagnostic' }) }}
        </div>
        <div class="col-12">
            {{ form_row(form.recommandations, { label: '📝 Recommandations' }) }}
        </div>
        <div class="col-12 col-md-6">
            {{ form_row(form.niveauGravite, { label: '⚠️ Niveau de gravité' }) }}
        </div>
        <div class="col-12">
            {{ form_row(form.fichier, { label: '📎 Fichier' }) }}
        </div>
    </div>
    <div class="mt-4 d-flex gap-2">
        <button class="btn btn-primary" type="submit">{{ submit_label|default('Enregistrer') }}</button>
        <a class="btn btn-light" href="{{ path('consultation_index') }}">Retour</a>
    </div>
{{ form_end(form) }}
