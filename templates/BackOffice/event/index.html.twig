{% extends 'base.html.twig' %}
{% block title %}Gestion Événements{% endblock %}

{% block stylesheets %}
  {{ parent() }}
  <link rel="stylesheet" href="{{ asset('css/sb-admin-2.min.css') }}">
  <link rel="stylesheet" href="{{ asset('css/forum.css') }}">
{% endblock %}

{% block body %}
<div class="forum-wrap">
  <div class="container">

    <div class="forum-header">
      <div>
        <h2 class="forum-title">Événements</h2>
        <div class="text-muted">
          BackOffice Elder<span style="color:var(--brand-orange);font-weight:900;">Health</span>Care
        </div>
      </div>

      <div class="d-flex gap-2">
        <a class="btn btn-outline-primary" href="{{ path('event_type_index') }}">
          <i class="bi bi-tags"></i> Types
        </a>
        <a class="btn btn-brand" href="{{ path('event_new') }}">
          <i class="bi bi-plus-circle"></i> Nouvel événement
        </a>
      </div>
    </div>

    {# flash #}
    {% for label, messages in app.flashes %}
      {% for msg in messages %}
        <div class="alert alert-{{ label }} mb-3">{{ msg }}</div>
      {% endfor %}
    {% endfor %}

    {% for e in events %}
    {% set typeColor = e.type and e.type.couleur ? e.type.couleur : '#6c757d' %}
<div class="post-card" style="border-left: 8px solid {{ typeColor }};">

        <div class="post-card__top">
          <div class="post-meta">
            <h5 class="mb-1">{{ e.titre }}</h5>

            <div class="small">
              <i class="bi bi-calendar-event"></i>
              {{ e.dateDebut ? e.dateDebut|date('d/m/Y H:i') : '—' }}
              {% if e.dateFin %}
                → {{ e.dateFin|date('d/m/Y H:i') }}
              {% endif %}
              <span class="ms-2">
                <i class="bi bi-tag"></i>
{{ e.type ? e.type.libelle : 'Sans type' }}
{% if e.type and e.type.couleur %}
  <span class="ms-2 badge-color" style="background: {{ e.type.couleur }};"></span>
{% endif %}              </span>
            </div>

            {% if e.lieu %}
              <div class="small text-muted mt-1">
                <i class="bi bi-geo-alt"></i> {{ e.lieu }}
              </div>
            {% endif %}
          </div>

          <span class="badge-soft {{ e.statut == 'PUBLIE' ? 'badge-active' : 'badge-inactive' }}">
            {{ e.statut|default('BROUILLON') }}
          </span>
        </div>

        <div class="post-card__body">
          <p class="post-content">{{ e.description|default('') }}</p>

          <div class="d-flex flex-wrap gap-3 mt-2 small">
            {% if e.capaciteMax %}
              <span><i class="bi bi-people"></i> Capacité : {{ e.capaciteMax }}</span>
            {% endif %}
         {% if e.image %}
  <div class="mt-2">
    <img src="{{ asset('uploads/events/' ~ e.image) }}" 
         alt="Image event" 
         style="max-width:150px;border-radius:8px;">
  </div>
{% endif %}

          </div>
        </div>

        <div class="actions">
          <a class="icon-btn" title="Modifier" href="{{ path('event_edit', {id: e.id}) }}">
            <i class="bi bi-pencil-square"></i>
          </a>

          {% include 'BackOffice/event/_delete_form.html.twig' with {event: e} %}

          <form method="post" action="{{ path('event_participer', {id: e.id}) }}" class="d-inline">
            <input type="hidden" name="_token" value="{{ csrf_token('participer_event_' ~ e.id) }}">
            <button type="submit" class="btn btn-outline-primary btn-pill btn-sm">
              <i class="bi bi-check2-circle"></i> Participer
            </button>
          </form>

          <form method="post" action="{{ path('event_annuler', {id: e.id}) }}" class="d-inline">
            <input type="hidden" name="_token" value="{{ csrf_token('annuler_event_' ~ e.id) }}">
            <button type="submit" class="btn btn-outline-danger btn-pill btn-sm">
              <i class="bi bi-x-circle"></i> Annuler
            </button>
          </form>
        </div>

      </div>
    {% else %}
      <div class="empty">Aucun événement pour le moment 🙂</div>
    {% endfor %}

  </div>
</div>
{% endblock %}
